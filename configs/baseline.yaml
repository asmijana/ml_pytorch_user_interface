experiment:
  name: baseline_mlp_california_housing
data:
  out_dir: data
  test_size: 0.14
  val_size: 0.1
  random_state: 42
model:
  hidden_dims:
  - 128
  - 64
  dropout: 0.1
train:
  lr: 0.001
  batch_size: 256
  max_epochs: 200
  weight_decay: 0.0
  patience: 20
  min_delta: 0.0
  num_workers: 0
runtime:
  seed: 7
  device: auto
  use_amp: false
